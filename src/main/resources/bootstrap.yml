spring:
  application:
    name: kv
  profiles:
    active:
      - test_db
  cloud:
    server:
      vault:
        authentication: TOKEN
        token: hvs.dXRm0m31APpp6iId5DOYQsIH
        scheme: http
        host: localhost
        port: 8200
        config:
          order: 1
        connection-timeout: 5000
        read-timeout: 15000
